"""Foundation-owned runtime: model gateway, session/audit persistence, platform clients.

Mirrors the "Analytics Foundation" ownership boundary in
``agentWorkflows/mcp-oss-target-architecture.puml``. Nothing here knows about OPO
business semantics; that stays in ``application_ui``.
"""
