"""Foundation platform clients: implementation behind the MCP capability adaptor.

Ported from ``AnalyticsFoundation/*_client.py`` and ``lanadb_query.py`` /
``datawarehouse.py`` with no behavior changes, so the MCP servers in
``mcp_capability_adaptor/`` have a stable backend. See PLAN.md Phase 3.
"""
