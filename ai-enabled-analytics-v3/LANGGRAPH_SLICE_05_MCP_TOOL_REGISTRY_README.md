# Slice 05: generic MCP tool registry

Adds domain-neutral MCP tool keys, schemas, atomic discovery refresh, registry
lookup, and agent allowlist resolution. It does not add concrete transport,
platform API names, authorization fields, or LangGraph ToolNode integration.
