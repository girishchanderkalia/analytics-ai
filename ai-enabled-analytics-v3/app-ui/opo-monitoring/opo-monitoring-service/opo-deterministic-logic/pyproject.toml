[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "opo-deterministic-logic"
version = "0.1.0"
description = "Pure deterministic OPO calculations"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
