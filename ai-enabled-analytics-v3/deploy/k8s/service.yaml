apiVersion: v1
kind: Service
metadata:
  name: application-agent-runtime
  namespace: ai-agents
spec:
  selector:
    app.kubernetes.io/name: application-agent-runtime
  ports:
    - name: http
      port: 8000
      targetPort: http
