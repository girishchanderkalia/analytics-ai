apiVersion: apps/v1
kind: Deployment
metadata:
  name: application-agent-runtime
  namespace: ai-agent-fwk
  labels:
    app.kubernetes.io/name: application-agent-runtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: application-agent-runtime
  template:
    metadata:
      labels:
        app.kubernetes.io/name: application-agent-runtime
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 100
      containers:
        - name: runtime
          image: REPLACE_WITH_RUNTIME_IMAGE
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: application-agent-runtime-config
            - secretRef:
                name: application-agent-runtime-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: runtime-data
              mountPath: /app/runtime-data
            - name: tmp
              mountPath: /tmp
            - name: ca-bundle
              mountPath: /etc/ssl/asml/combined-ca-bundle.pem
              subPath: combined-ca-bundle.pem
              readOnly: true
      volumes:
        - name: runtime-data
          persistentVolumeClaim:
            claimName: application-agent-runtime-data
        - name: tmp
          emptyDir: {}
        - name: ca-bundle
          configMap:
            name: application-agent-runtime-ca-bundle
